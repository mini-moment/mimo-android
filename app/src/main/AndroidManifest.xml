<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">
    <!-- 위치권한 -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" /> <!-- 인터넷 권한 -->
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".core.MimoApplication"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/mini_moment_icon"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/mini_moment_icon_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MiniMoment"
        android:usesCleartextTraffic="true"
        tools:targetApi="31">
        <activity
            android:name="com.mimo.presentation.upload_video.UploadVideoActivity"
            android:exported="false"
            android:windowSoftInputMode="adjustPan" />
        <!-- NaverMap -->
        <meta-data
            android:name="com.naver.maps.map.CLIENT_ID"
            android:value="${NAVER_MAP_CLIENT_KEY}" />

        <activity
            android:name="com.mimo.presentation.login.LoginActivity"
            android:exported="true"
            android:theme="@style/Theme.MiniMoment.Splash">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name="com.mimo.presentation.MainActivity"
            android:exported="false" />
        <activity
            android:name="com.mimo.presentation.video_detail.VideoDetailActivity"
            android:exported="false" />
    </application>

</manifest>